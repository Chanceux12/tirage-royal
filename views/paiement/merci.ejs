<style>
/* Conteneur principal de la page */
.vantex-merci-wrapper {
  min-height: calc(100vh - 200px); /* header + footer */
  display: flex;
  justify-content: center;
  align-items: flex-start;
  padding: 60px 16px;
}

.vantex-status-badge {
  display: inline-block;
  margin: 14px auto 6px;
  padding: 6px 14px;
  font-size: 0.85rem;
  font-weight: 600;
  color: #0A9999;
  background: rgba(10, 153, 153, 0.12);
  border: 1px solid rgba(10, 153, 153, 0.35);
  border-radius: 999px;
  letter-spacing: 0.2px;
}

/* Carte */
.vantex-merci-card {
  background: #fff;
  width: 100%;
  max-width: 520px;
  padding: 32px 26px;
  border-radius: 18px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.12);
  text-align: center;
}

/* Titre */
.vantex-merci-card h1 {
  color: #0A9999;
  font-size: 1.6rem;
  margin-bottom: 18px;
}

/* Alerte */
.vantex-merci-alert {
  background: #f0f8ff;
  border-left: 5px solid #0A9999;
  padding: 16px;
  margin: 20px 0;
  border-radius: 10px;
  font-size: 0.95rem;
  text-align: left;
  color: #111;              
}


/* Texte */
.vantex-merci-card p {
  color: #333;
  font-size: 0.95rem;
  line-height: 1.6;
  margin-bottom: 14px;
}

/* Bouton */
.vantex-merci-btn {
  display: block;
  width: 100%;
  max-width: 100%;
  background: #0A9999;
  color: #fff;
  padding: 15px;
  text-decoration: none;
  border-radius: 12px;
  font-size: 1rem;
  margin: 24px auto 0;   /* 🔥 CENTRAGE RÉEL */
  text-align: center;   /* 🔥 TEXTE CENTRÉ */
  box-sizing: border-box;
}


.vantex-merci-btn:hover {
  background: #087a7a;
}

@keyframes softPulse {
  0% { box-shadow: 0 0 0 0 rgba(10,153,153,0.3); }
  70% { box-shadow: 0 0 0 8px rgba(10,153,153,0); }
  100% { box-shadow: 0 0 0 0 rgba(10,153,153,0); }
}

.vantex-status-badge {
  animation: softPulse 2.5s infinite;
}

</style>

<div class="vantex-merci-wrapper">
  <div class="vantex-merci-card">

    <h1>Merci pour votre demande !</h1>

    <div class="vantex-status-badge">
      En attente de validation
    </div>


    <div class="vantex-merci-alert">
       <strong>Votre demande d’ouverture de compte VANTEX a bien été enregistrée.
       Elle est actuellement en cours de vérification par nos équipes</strong>.
    </div>

    <p>Vous serez informé(e) par email dès que votre demande aura été traitée.
     Cette procédure permet de garantir la sécurité et la conformité de nos services.</p>

    <p>Une fois votre compte validé, vous pourrez accéder à l’ensemble des fonctionnalités VANTEX.</p>

    <p>
      <strong>Note :</strong>
      Une seule demande par email et numéro de téléphone est autorisée tant que la précédente est en attente.
    </p>

    <a href="/" class="vantex-merci-btn">Retour à l'accueil</a>

  </div>
</div>
