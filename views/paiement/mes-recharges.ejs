<!DOCTYPE html> 
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mes recharges</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      padding: 1rem;
    }

    .container {
      max-width: 900px;
      margin: 2rem auto;
      border-radius: 10px;
    }

    h1 { margin-bottom: 1rem; }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 1rem;
    }

    th, td {
      padding: 0.8rem 1rem;
      text-align: left;
      border-bottom: 1px solid rgba(0,0,0,0.1);
    }

    th {
      font-weight: bold;
      border-bottom: 2px solid rgba(0,0,0,0.2);
    }

    tr:hover {
      background: rgba(0,0,0,0.03);
    }

    /* Statuts colorés */
    .status-en_attente { color: #f59e0b; font-weight: bold; }
    .status-echoue { color: #ef4444; font-weight: bold; }
    .status-reussi { color: #10b981; font-weight: bold; }

    /* Boutons */
    .buttons {
      margin-top: 1.5rem;
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .buttons a {
      display: inline-block;
      padding: 0.7rem 1.2rem;
      border-radius: 6px;
      text-decoration: none;
      font-weight: bold;
      text-align: center;
      transition: all 0.3s ease;
    }

    .btn-nouveau { background: #2563eb; color: #fff; }
    .btn-nouveau:hover { background: #1e4bb8; }

    .btn-historique { background: #6b7280; color: #fff; }
    .btn-historique:hover { background: #4b5563; }

    /* Responsive */
    @media(max-width:768px){
      table, th, td { font-size: 0.85rem; padding: 0.6rem; }
      .buttons { flex-direction: column; }
    }

    @media(max-width:480px){
      table, th, td { font-size: 0.8rem; padding: 0.5rem; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Mes recharges</h1>

    <% if (recharges.length === 0) { %>
      <p>Vous n'avez encore effectué aucune recharge.</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Date</th>
            <th>Montant</th>
            <th>Devise</th>
            <th>Méthode</th>
            <th>Référence</th>
            <th>Statut</th>
          </tr>
        </thead>
        <tbody>
          <% recharges.forEach(r => { %>
            <tr>
              <td><%= r.date %></td>
              <td><%= r.amount.toFixed(2) %></td>
              <td><%= r.currency %></td>
              <td><%= r.method %></td>
              <td><%= r.reference %></td>
              <td class="status-<%= r.statut.replace('é','e') %>"><%= r.statut %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <div class="buttons">
      <a href="/paiement/recharger" class="btn-nouveau">Nouvelle recharge</a>
      <a href="/paiement/mes-recharges" class="btn-historique">Voir l'historique</a>
    </div>
  </div>
</body>
</html>

