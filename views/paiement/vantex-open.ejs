<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Formulaire</title>

<style>
/* --- CONTENEUR GLOBAL --- */
.page-wrapper {
    display: flex;
    flex-direction: row;
    width: 100%;
    min-height: 100vh;
}

/* --- IMAGE À GAUCHE SUR ORDINATEUR --- */
.image-side {
    width: 45%;
    background-image: url("https://picsum.photos/900/1200"); /* 🔥 Mets ton image ici */
    background-size: cover;
    background-position: center;
}

/* --- FORMULAIRE --- */
.form-container {
    width: 55%;
    padding: 40px;
    background: #fff;
}

/* Champs */
.form-group {
    margin-bottom: 20px;
}

label {
    display: block;
    font-size: 14px;
    margin-bottom: 6px;
}

input, select {
    width: 100%;
    padding: 12px;
    font-size: 16px;
    border: 1px solid #ccc;
    border-radius: 6px;
}

/* --- CHAMP TÉLÉPHONE EXACTEMENT COMME TON IMAGE --- */
.phone-wrapper {
    display: flex;
    align-items: center;
    border: 1px solid #ccc;
    border-radius: 6px;
    overflow: hidden;
}

.phone-flag {
    display: flex;
    align-items: center;
    background: #f5f5f5;
    padding: 8px 12px;
    gap: 6px;
    border-right: 1px solid #ccc;
    font-size: 15px;
}

.phone-flag img {
    width: 22px;
    height: 16px;
    object-fit: cover;
}

.phone-input {
    border: none;
    flex: 1;
    padding: 12px;
    font-size: 16px;
    outline: none;
}

/* --- MOBILE : image en haut façon bannière --- */
@media (max-width: 768px) {
    .page-wrapper {
        flex-direction: column;
    }

    .image-side {
        width: 100%;
        height: 180px;
    }

    .form-container {
        width: 100%;
        padding: 20px;
    }
}
</style>
</head>

<body>

<div class="page-wrapper">

    <!-- 🔥 IMAGE À GAUCHE (ordinateur) — EN BANNIÈRE SUR MOBILE -->
    <div class="image-side"></div>

    <!-- 🔥 FORMULAIRE -->
    <div class="form-container">

        <h2>Formulaire d'inscription</h2>

        <!-- CIVILITÉ -->
        <div class="form-group">
            <label for="civilite">Civilité</label>
            <select id="civilite">
                <option value="M.">M.</option>
                <option value="Mme">Mme</option>
                <option value="Mlle">Mlle</option>
                <option value="Dr">Dr</option>
                <option value="Pr">Pr</option>
            </select>
        </div>

        <!-- NOM -->
        <div class="form-group">
            <label>Nom</label>
            <input type="text">
        </div>

        <!-- PRÉNOM -->
        <div class="form-group">
            <label>Prénom</label>
            <input type="text">
        </div>

        <!-- TÉLÉPHONE -->
        <div class="form-group">
            <label>Téléphone</label>

            <div class="phone-wrapper">
                <div class="phone-flag">
                    <img src="https://flagcdn.com/w20/fr.png" alt="">
                    +33
                </div>
                <input class="phone-input" type="tel" placeholder="Téléphone">
            </div>

        </div>

        <button style="
            width:100%;
            padding:15px;
            font-size:17px;
            border:none;
            border-radius:6px;
            background:#0066ff;
            color:#fff;
            cursor:pointer;">
            S'inscrire
        </button>

    </div>
</div>

</body>
</html>


